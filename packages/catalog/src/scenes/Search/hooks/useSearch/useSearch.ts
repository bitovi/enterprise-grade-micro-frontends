export const useSearch = (): {
  search: string;
  setSearch: (newSearch: string) => void;
} => {
  // Placeholders remove once ready
  const search = "";
  const setSearch = (newString: string) => {};
  //

  return {
    search,
    setSearch,
  };
};
